# `SoundboardWeb.Components.Soundboard.EditModal`

The edit modal component.

# `edit_modal`

## Attributes

* `flash` (`:map`) - Defaults to `%{}`.
* `edit_name_error` (`:string`) - Defaults to `nil`.
* `current_user` (`:map`) (required)
* `current_sound` (`:map`) (required)
* `tag_input` (`:string`) - Defaults to `""`.
* `tag_suggestions` (`:list`) - Defaults to `[]`.

---

*Consult [api-reference.md](api-reference.md) for complete listing*
